rootProject.name = "hello-native"

pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        jcenter()
        maven ("https://dl.bintray.com/kotlin/kotlin-eap")
    }
}